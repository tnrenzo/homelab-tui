# homelab-tui

homelab monitoring in the terminal. uses bubble tea for the ui, websockets sending json for live updates.

 to start server:
```
go run ./backend
```

to start gui:
```
go run ./frontend
```
